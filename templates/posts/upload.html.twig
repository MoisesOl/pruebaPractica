{% extends 'base.html.twig' %}

{% block title %}Subir post{% endblock %}

{% block body %}
    <div class="col-lg-8 mb-4 ml-auto mr-auto">

        <!-- Card -->
        <div class="card">

            <!-- Card image -->
            <div class="card-header">
                <h5 class="mb-0 font-weight-bold">Subir post</h5>
            </div>
            <!-- Card image -->

            <!-- Card content -->
            <div class="card-body card-body-cascade text-center">

                <!-- Edit Form -->
                <form method="POST" action="{{ path('app_posts_new') }}" enctype="multipart/form-data">
                    {#
                    <div class="form-group">
                        <label for="titulo">Titulo</label>
                        <input type="text" class="form-control" id="titulo" placeholder="Titulo del Post">
                    </div>
                    <div class="form-group">
                        <label for="descripcion">Descripción</label>
                        <textarea class="form-control" id="descripcion" placeholder="Descripción del Post" rows="4"></textarea>
                    </div>
                    #}
                    <div class="form-group custom-file">
                        <input type="file" class="custom-file-input" name="image">
                        <label class="custom-file-label" for="image">Selecciona un archivo</label>
                    </div>
                    <button type="submit" class="btn btn-primary">Submit</button>
                </form>

            </div>
            <!-- Card content -->

        </div>
        <!-- Card -->

    </div>
    <!-- Second column -->
{% endblock %}
{% block javascripts %}
    {{ parent() }}
    <script>
        {#  Código para que muestre el nombre del archivo que se adjuntó  #}
        $(".custom-file-input").on("change", function() {
            var fileName = $(this).val().split("\\").pop();
            $(this).siblings(".custom-file-label").addClass("selected").html(fileName);
        });
    </script>
{% endblock %}